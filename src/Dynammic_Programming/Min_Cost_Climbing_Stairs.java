package Dynammic_Programming;
import java.util.*;
public class Min_Cost_Climbing_Stairs {
//    public int helper(int i, int[] arr, int[] cost){
//        if(i>=arr.length) return 0;
//        if(arr[i]!=-1) return arr[i];
//        return arr[i] = cost[i] + Math.min(helper(i+1,arr,cost),helper(i+2,arr,cost));
//    }
//    public int minCostClimbingStairs(int[] cost) {
//        int[] arr = new int[cost.length];
//        Arrays.fill(arr,-1);
//        return Math.min(helper(0,arr,cost),helper(1,arr,cost));
//    }

    //By Iterative Dynammic Programming
//    public int minCostClimbingStairs(int[] cost) {
//
//    }
}
